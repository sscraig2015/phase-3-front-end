import React from "react"

function NewAddition({category}){
    return (
        <div>
            <input type="text"/>
            <input type="submit" value="Submit"/>
            {category === "courses" ? }
        </div>  
    )

}

export default NewAddition;